--- a/src/ap/ieee802_11_eht.c
+++ b/src/ap/ieee802_11_eht.c
@@ -272,10 +272,14 @@
 		oper->oper_params |= EHT_OPER_DEFAULT_PE_DURATION;
 
 	/* TODO: Fill in appropriate EHT-MCS max Nss information */
-	oper->basic_eht_mcs_nss_set[0] = 0x11;
-	oper->basic_eht_mcs_nss_set[1] = 0x00;
-	oper->basic_eht_mcs_nss_set[2] = 0x00;
-	oper->basic_eht_mcs_nss_set[3] = 0x00;
+	/* WLAN Pi Profiler: Advertise 4 SS support in EHT Operation element */
+	/* Format: bits 0-3 indicate max NSS, 0xf = 4 SS (bits 0,1,2,3 set) */
+	/* Each byte covers one MCS range: MCS 0-7, 8-9, 10-11, 12-13 */
+	oper->basic_eht_mcs_nss_set[0] = 0xff;  /* MCS 0-7: Rx max 4 SS, Tx max 4 SS */
+	oper->basic_eht_mcs_nss_set[1] = 0xff;  /* MCS 8-9: Rx max 4 SS, Tx max 4 SS */
+	oper->basic_eht_mcs_nss_set[2] = 0xff;  /* MCS 10-11: Rx max 4 SS, Tx max 4 SS */
+	oper->basic_eht_mcs_nss_set[3] = 0xff;  /* MCS 12-13: Rx max 4 SS, Tx max 4 SS */
+
 
 	if (!eht_oper_info_present)
 		return pos + elen;
